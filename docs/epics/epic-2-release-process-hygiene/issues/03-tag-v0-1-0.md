---
type: Issue
title: "Tag v0.1.0 with a minimal release note"
description: "Cut the first tagged release once every other item in the plan (both epics) has landed, so go get stops pinning a pseudo-version of main."
tags: [epic-2]
timestamp: 2026-07-09T16:30:00Z
resource: https://github.com/julienlegoux/kern-proxy/issues/97
epic: 2
issue: 3
slug: tag-v0-1-0
size: S
status: done
gh_issue: 97
depends_on: [1, 2]
---

# Tag v0.1.0 with a minimal release note

## Summary

No tags exist; `go get` currently pins a pseudo-version of `main`. Tag `v0.1.0`
with a minimal release note. The plan is explicit that this lands **last**,
after every other item in both epics.

## Scope

- Tag `v0.1.0` on the commit where all other plan items have landed on `main`.
- A minimal release note (GitHub Release): one-paragraph summary, headline items (retries honored everywhere, leak-free `Events(ctx)`, lint CI, credential-mode docs), and the pre-v1 breaking changes from Epic 1 (the `Events` signature and message pointer receivers).
- Verify `go get github.com/julienlegoux/kern-proxy@v0.1.0` resolves the tag.

## Out of scope

- Any code change. If something is broken at tagging time, fix it in its own issue first — this issue is only the release act.
- Changelog automation / release tooling — a hand-written note is fine at v0.1.0.

## Acceptance criteria / Definition of done

- **Gate:** all Epic 1 sub-issues (#87–#94) and Epic 2 issues 01–02 are closed before tagging.
- `v0.1.0` tag exists with a GitHub Release carrying the minimal note, including the breaking-change callouts.
- `go get ...@v0.1.0` resolves cleanly.

## Relevant files / areas

- Git tag + GitHub Release only; no repo files (unless the release note is also committed, which is not required).

## Dependencies

Blocked by [Issue 01](./01-golangci-lint-ci.md), [Issue 02](./02-credential-modes-docs.md),
and all of [Epic 1](/epic-1-reliability-api-fixes/EPIC_1.md) (#85). Must be the
last item in the plan to land.

## PR size note

Target ~500 changed lines; if this grows past ~1000, split it before opening the PR.
(This one is a tagging/release act — likely no PR at all, or a trivial one.)
