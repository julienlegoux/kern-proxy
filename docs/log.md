# Log

## 2026-08-08

* **Update**: Linked the new [planning bundle](planning/index.md) from the index. It is a separate OKF root under `docs/planning/`, keeping dev-process artifacts out of this consumer-facing bundle.

## 2026-07-09

* **Creation**: Added the post-review improvement plan (since removed; preserved in git history) capturing verified findings from an external adoption review — dead `MaxRetries` outside Codex, `Stream.Events()` goroutine leak, receiver consistency, session round-trip docs, lint CI, credential-risk documentation, and a `v0.1.0` tag.

* **Creation**: Rebuilt `docs/` as an OKF knowledge bundle after removing the completed dev-plan artifacts (plans, epics, implementation review — preserved in git history). Added [usage guide](usage.md), [architecture](architecture.md), and [auth & credentials](auth.md).
* **Update**: Restored [porting map](PORTING.md) from the archive, compressed to one-line statuses and pruned of resolved epic narratives.
